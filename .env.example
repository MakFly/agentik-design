# ── Next.js (apps/web) ─────────────────────────────────────────
# Workflow engine base URL the /api/* rewrite proxies to.
API_URL=http://localhost:8787

# ── Workflow engine (apps/engine) — see apps/engine/.env.example
