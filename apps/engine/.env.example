# Engine HTTP port (Next.js rewrites /api/* here in dev)
PORT=8787

# Postgres — dev: shared infra (infra-postgres on dev-shared-net / localhost:5432)
DATABASE_URL=postgresql://agentik:agentik@localhost:5432/agentik

# Redis — dev: shared infra (infra-redis / localhost:6379), used by BullMQ
REDIS_URL=redis://localhost:6379

# OpenAI key for the agent node (Phase 4). Optional until then.
OPENAI_API_KEY=
